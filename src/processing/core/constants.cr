module Processing
  module Core
    module Constants
      PI = ::Math::PI

      HALF_PI = PI / 2.0

      QUATER_PI = PI / 4.0

      TAU = ::Math::TAU

      TWO_PI = ::Math::PI * 2.0
    end
  end
end
